#include<bits/stdc++.h>
using namespace std;
int main(){

    int* ptr = NULL;

    cout << ptr << endl; // We cannot dereference the null pointer. Because it address to null 
    cout << *ptr << endl;
    return 0;
}