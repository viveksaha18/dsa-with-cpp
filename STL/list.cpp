#include<bits/stdc++.h>
using namespace std;
int main(){
    list<int>l;
    l.push_back(3);
    l.emplace_back(5);
    l.push_front(6);
    // all other function is similar to vectors 
  return 0;
}
